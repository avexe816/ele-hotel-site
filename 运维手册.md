# ELE HOTEL 官网 运维手册（v2.3）

网站地址：**https://ele-hotel.com**　｜　管理后台：**https://ele-hotel.com/admin**

---

## 这一版最大的变化：日常运营不再需要碰代码

v1.9 之前，改一句文案要在电脑上装 Python、改 JSON 文件、跑 `build.py`、再上传。
现在**所有日常运营都在浏览器里的管理后台完成**，翻译自动生成，改完点一下就上线。

所以这本手册重新分成了三部分，按你的角色读对应的部分就够了：

| 你是 | 读哪部分 | 需要的技能 |
|---|---|---|
| 负责改文案、换照片、加酒店的运营人员 | **第一部**（第 1〜10 节） | 会用浏览器就行 |
| 负责账号、域名、出故障时排查的管理者 | 第一部 + **第二部**（第 11〜17 节） | 会登录 Cloudflare / GitHub |
| 要改设计、加语言、改结构的开发者 | **第三部**（第 18〜27 节） | Python / Git |

> **v2.3 的变化**：① **お問い合わせ表单按类别自动分派收件人** —— 加了 エリア / ホテル 联动选择，
> 预约·住宿·遗失物类的咨询直接发到那家酒店的窗口邮箱，其余按类别发到 sales / hr / info（见第 8-7 节）；
> ② **首页搜索和各酒店预约区都能选「部屋数」**（最多 10 间），酒店详情页的预约区也改成
> チェックイン / チェックアウト 两个日期，和首页一致；
> ③ **顶部右上角新增「宿泊予約」按钮**（日历图标），首页点它跳到空室検索，酒店页点它跳到该店的预约区；
> 顶部菜单里的「お知らせ」已移除（首页公告区块和页脚链接都还在）；
> ④ **客室タイプ的卡片可以显示 4 行信息** —— 新增 ベッドサイズ 和 階数（见第 7 节）。
>
> **v2.2 的变化**：① **顶部菜单和页脚链接可以分开在后台增删改** —— 新增菜单组
> 「メニュー（ヘッダー・フッター）」，ヘッダーメニュー（画面上部）和 フッターのリンク（インフォメーション欄）
> 各自一份列表，能加项目、改名字、改链接、上下调顺序、删除（见第 8-5 节）；
> ② 页脚已加入 **採用情報**，指向 `https://en-gage.net/tej_saiyo/`，外部链接会自动在新标签页打开并带小箭头图标；
> ③ **可以自由新增页面** —— 左侧新增「ページ」组，能建「会社案内」「利用規約」等任意页面，
> URL 为 `/pages/<名字>.html`，保存后自动生成 5 种语言（见第 8-6 节）。
>
> **v2.1 的变化**：① **予約プロ 预约表单现在任何一家酒店都能在后台自己接入** —— 在酒店编辑页的
> 「オンライン予約フォーム（予約プロ）」里选「予約プロを使う」，填入施设ID（或直接粘贴予約プロ的网址），
> 保存后那家酒店的详情页就会出现和樟葉一样的搜索表单，人数 / 泊数 / 部屋数 的上限也能自己设；
> ② 首页搜索框下方那句「可直接在线预订的酒店：…」现在**根据后台设置自动生成**，不用手改；
> ③ 新增独立菜单 **「GRAND ELE ページ」**，Grand ELE Hotel 专页的 56 项内容（含所有照片）全部可后台编辑；
> ④ 首页主视觉大图、品牌区块照片改成后台可换的「画像ID」字段；
> ⑤ 每家酒店可单独设置 **SEO 的页面标题 / 说明文**（留空则自动生成）；
> ⑥ **エリア（地区筛选按钮）可以自己增删** —— 想加「福岡エリア」不用找开发者；
> ⑦ 酒店新增 **FAX番号** 栏；「ブランド区分」由手打改成下拉选择。
>
> **v2.0 的变化**：① 新增管理后台 `/admin`：文字编辑、5 语言译文校对、图片上传、酒店增删排序，
> 保存后自动构建并上线；② 接入 Cloudflare Workers AI 自动翻译，日文改完自动补齐中英韩；
> ③ 源码迁到 GitHub 私有仓库，改用 GitHub Actions 自动部署（不再手工上传）；
> ④ 主机从静态托管迁到 **Cloudflare Pages**，域名 `ele-hotel.com` 已生效；
> ⑤ 空字段统一显示「準備中」，「開業予定」的提示文改成读各酒店自己的「開業」栏。

> **v1.9 的变化**：详情页「設備」改名「設備・アメニティ」并拆成 **客室設備・館内設備** 与
> **備品・アメニティ** 两组；7 家酒店设施图标统一顺序；施設名统一为 ELE Hotel 樟葉；
> 删除首页最下方的 TEJ 品牌说明段落；お問い合わせ 咨询类别调整。
>
> **v1.8**：新增独立的 お問い合わせ 页面（5 语言），表单经 Lark 邮箱 SMTP 发到 info@ele-hotel.com。
>
> **v1.7**：6 家开业酒店换成真实照片；新增 5 语言隐私政策页；樟葉店接入 予約プロ 预约引擎。
>
> **v1.5**：`data/` 里只写日文原文，其他语言由翻译库自动套用。

---

# 第一部　日常运营（在后台完成）

## 1. 登录后台

打开 **https://ele-hotel.com/admin**，需要两样东西：

| 项目 | 说明 |
|---|---|
| **メールアドレス** | 必须是白名单里登记过的邮箱。没登记的邮箱输对密码也进不去 |
| **パスワード** | 全员共用的一个密码 |

- 密码和白名单由管理者在 Cloudflare 上设置，见 **第 12 节**。
- 输错时统一提示「メールアドレスまたはパスワードが違います」，不会告诉你是哪个错了（防止别人试出有效邮箱）。输错会故意等约 1 秒才回应。
- 登录后 **12 小时**有效，之后要重新登录。
- 后台页面对搜索引擎设了 `noindex`，不会被 Google 收录。

> **安全提醒**：白名单里的邮箱一旦从 Cloudflare 删掉，那个人**当场**就被踢出去（每次请求都会重新核对），不用等 12 小时。员工离职时删邮箱就行。

---

## 2. 界面结构

登录后是左右两栏。

### 左侧菜单（从上往下）

**网站各页面的文字**（11 组）

| 菜单项 | 管什么 |
|---|---|
| トップページ | 首页主视觉、标语、搜索框、酒店一览区块的文案 |
| 客室・設備セクション | 首页的客室・設備介绍区块 |
| お知らせ | 首页公告 |
| よくあるご質問 | FAQ 问答 |
| ブランドページ | 首页的品牌介绍区块（四条产品线的说明、照片） |
| GRAND ELE ページ | Grand ELE Hotel 专页 `/grand` 的全部内容（56 项：SEO、标题、房型、餐饮、服务、7 张照片…） |
| お問い合わせページ | 联系页的标题、字段名、咨询类别、右侧说明框 |
| プライバシーポリシー | 隐私政策全文 |
| ホテル詳細ページの見出し | 各酒店详情页共用的小标题（施設情報 / アクセス / 地図、以及客室卡片的 広さ・仕様 / 定員 / ベッドサイズ / 階数） |
| 共通・ナビ・フッター | 页脚底部的 **著作権表示の年**（例 `2019–2026`）、**著作権表示**（株式会社TEJ）、**フッター右下の都市名**（留空则按有酒店的地区自动生成 Tokyo · Nagoya · …），以及导航和页脚的共用文案 |
| 予約ブロック | 首页空室検索 和 详情页预约区的文案（エリア / チェックイン / チェックアウト / 人数 / 部屋数 等的项目名和单位） |
| メニュー（ヘッダー・フッター） | 顶部菜单、页脚链接的项目本身（增删改、排序）见第 8-5 节。右上角「宿泊予約」按钮的文字也在这一组。首页「採用について」那句话和页脚「採用情報」的跳转地址，也在这一组的「採用情報のリンク先URL」里改（留空则不做成链接） |
| 共通・ナビ・フッター | 导航、页脚、按钮、语言菜单等全站共用文字 |

**画像** —— 上传照片、查看已有照片一览（见第 5 节）

**ホテル**

- **追加・並び替え・削除** —— 加新酒店、调一览顺序、删酒店（见第 8 节）
- 下面依次是 7 家酒店，点进去编辑那家店的全部资料

**ページ**

- **追加・並び替え・削除** —— 自由新增页面（会社案内、利用規約 等），见第 8-6 节
- 下面依次是已建好的页面，点进去编辑内容

菜单项右边有**小圆点**时，表示这一组里有未保存的改动。

### 右侧编辑区

最上方是 **5 个语言页签**：

```
日本語 │ 简体中文 │ 繁體中文 │ English │ 한국어
```

**这是整个后台最重要的一条规则：**

> **只在「日本語」页签里写内容。其他 4 个页签是给你校对译文用的。**

日文是原文，其他语言全部由日文自动翻译而来。所以：

- 想改内容 → 切到 **日本語**，改完保存，其他语言自动跟着变
- 觉得某句翻译不好 → 切到那个语言，直接改那一句（见第 4 节）

### 最下方的保存栏

一直贴在页面底部，显示「未保存 N件」或「未保存の変更はありません」，右边是保存按钮。

---

## 3. 改文字

1. 左侧点要改的那一组（或那家酒店）
2. 确认语言页签是 **日本語**
3. 直接在输入框里改。改过的字段旁边会出现一个小点
4. 点右下角 **「変更内容を確認」**，会弹出一张对照表（改之前 → 改之后），逐条核对
5. 确认无误点 **「保存して公開」**

保存后大约 **1 分钟**网站就更新了。这一分钟里系统在做三件事：自动翻译 → 重新生成 55 个页面 → 部署上线。

### 输入框的几种类型

| 长相 | 怎么用 |
|---|---|
| 单行输入框 | 直接打字 |
| 多行输入框 | 会随内容自动变高 |
| 带 `↑ ↓ ×` 的列表 | 每行一项，`↑↓` 调顺序、`×` 删除、下方 **＋ 追加** 加一行（アクセス、周辺スポット、設備、アメニティ 都是这种） |
| 下拉选择 | エリア、公開状態、ギャラリーのキャプション |
| 灰色不可改 | `URL（変更不可）` —— 网址一旦公开就不该再改，避免死链 |

### 留空会怎样

留空的字段在网站上显示 **「準備中」**，不会出现空白或 `0`。所以拿不到准确信息时**请留空，不要瞎填**。

### 「公開状態」的两个值

| 值 | 效果 |
|---|---|
| 公開中 | 正常显示 |
| 開業予定 | 卡片和详情页会加「開業準備中」标签，详情页顶部显示开业提示 |

开业提示的文字取自这家酒店自己的 **「開業」** 栏（例：名古屋栄駅前 = `2026年9月開業予定`）。
「開業」栏留空时，才显示通用文案「開業日は決まり次第お知らせいたします。」

---

## 4. 多语言译文的校对与修改

### 平时不用管

在日本語页签改完保存，系统会自动把新写的日文翻成简体中文、繁體中文、English、한국어。
**你什么都不用做。**

### 想检查或修正某一句

切到那个语言的页签，每个字段下面会看到：

```
┌─────────────────────────────┐
│ 银座东                       │  ← 译文（可以直接改）
├─────────────────────────────┤
│ [人工確認済み]  [日本語から再翻訳] │
├─────────────────────────────┤
│ 銀座イースト                  │  ← 灰色的日文原文，供对照
└─────────────────────────────┘
```

标记的含义：

| 标记 | 含义 |
|---|---|
| **自動翻訳** | 机器翻的，还没人看过 |
| **人工確認済み** | 有人确认或改过，之后自动翻译**不会再覆盖**它 |
| **手直しあり** | 你刚改了这一句，还没保存 |
| **再翻訳します** | 你点了「日本語から再翻訳」，保存时会丢掉现在的译文重新翻 |

**改一句译文**：直接在输入框里改 → 保存。改过的会自动变成「人工確認済み」，以后不会被机器覆盖。

**觉得机器翻得比自己改的好**：点「日本語から再翻訳」→ 保存，会重新翻一次。

### 三条要记住的规则

1. **改了日文，所有语言的译文会重新翻。**
   系统是按日文原句来找译文的，日文一变就是一句「新句子」，之前人工改过的译文对它不再适用。所以**日文定稿后再花时间校对译文**，顺序反了会白做。

2. **繁體中文是从简体自动转的**，用的是台湾正体 + 港台惯用词表（酒店→飯店、信息→資訊、前台→櫃台、日元→日圓、智能马桶→免治馬桶 等）。所以**改中文时改简体那一份**，繁体会跟着变。只有某句繁体确实要跟简体不同说法时，才单独去繁體页签改。

3. **酒店名、地名、车站名有固定译法**，已经登记在系统里（銀座イースト→银座东 / Ginza East / 긴자 이스트，樟葉→樟叶 / Kuzuha / 구즈하 等）。这些不会被机器乱翻。如果发现有新的专有名词翻错了，改一次并保存（变成「人工確認済み」）就永久固定了。

### 列表类字段的译文

アクセス、設備、アメニティ 这类多行列表，在译文页签里会**每行一个输入框**，前面标着行号。
**行的增减只能在日本語页签做** —— 译文页签里会提示「項目の追加・削除・並べ替えは日本語タブで行ってください。」

### 房型卡片的译文

「客室タイプ（カード）」的 **名称** 和 **仕様** 也可以在译文页签校对（跟其他字段一样有标记和再翻译按钮）。
**定員** 和 **画像ID** 不需要翻译，所以在译文页签里是灰的。

---

## 5. 上传照片

左侧点 **「画像」**。

### 上传

1. 把照片**拖进**上方的虚线框，或者点框里的按钮选文件（可以一次选多张）
2. 每张照片会自动变成一行，显示压缩结果，例如
   `3200×1929px・元 1012 KB → 170 KB + 59 KB`
3. 检查一下自动起好的**图片ID**，需要的话改掉
4. 点 **「アップロードして公開」**

浏览器会在你本地把照片转成 webp 并生成两个尺寸，然后一次性上传。约 1 分钟后生效。

### 图片ID 的规则

系统会拿文件名自动生成，例如 `ELE Room Photo 01.JPG` → `ele-room-photo-01`。重名时自动加 `-2`。

| 规则 | 说明 |
|---|---|
| 只能用**半角小写字母、数字、连字符** | 日文、空格、大写、下划线都不行 |
| 3〜60 个字符 | |
| **不能以 `-sm` 结尾** | `-sm` 是系统给手机版留的后缀 |
| 一次上传里不能重名 | 会标红并禁止上传 |
| 跟已有照片同名 → 显示黄色警告「既存の画像を上書きします」 | 这是**允许**的，就是替换旧照片的正常做法 |

有红色错误时上传按钮会变灰，改好就自动恢复。

### 建议的照片规格

- 横向 3:2 或 16:10，**宽度 2000px 以上**
- jpg / png / HEIC 转出的 jpg 都行，系统统一转成 webp
- 系统自动生成两档：**电脑用 1800px** + **手机用 900px**（文件名后面加 `-sm`），你只要上传一张大图

### 命名习惯（建议遵守）

| 用途 | 格式 | 例 |
|---|---|---|
| 酒店主图 | `p-<酒店slug>-hero` | `p-kuzuha-hero` |
| 画廊照片 | `p-<酒店slug>-<用途>` | `p-kuzuha-lobby`、`p-kuzuha-bath` |
| 房型照片 | `p-<酒店slug>-<房型>` | `p-kuzuha-single`、`p-kuzuha-double` |

### 替换旧照片

**用同一个图片ID重新上传就行**，网站上所有用到它的地方一起换掉，不用去改任何字段。

### 照片一览

上传框下面是全部已有照片的缩略图和ID，可以搜索。点ID可以复制。

> **删除照片**：后台目前不提供删除。用不到的照片留着不会显示在网站上，也几乎不占成本。确实要清理请找开发者。

---

## 6. 给字段换照片

凡是标着 **「画像ID」** 的字段，右边都有一个 **「画像を選ぶ」** 按钮和一个小预览图。

点开后是全部照片的缩略图列表：

- 可以搜索
- 当前正在用的那张有高亮框
- 点一张就选中
- **「画像を外す」** 清空这个字段（网站上会显示「写真準備中」占位图）
- **「新しい写真を追加…」** 直接跳到「画像」页去上传

> 房型卡片**没有照片时请留空**，不要拿别的房型的照片顶替 —— 留空会显示「写真準備中」，比放错照片好。

---

## 7. 房型・画廊・设施的编辑

都在各酒店的编辑页里，都是**日本語页签**才能增删。

### 客室タイプ（カード）

每个房型一张卡片，卡片右上角 `↑ ↓ ×`：

| 栏 | 填什么 |
|---|---|
| 名称 | 例：シングル、セミダブル、ツイン |
| 仕様 | 例：`9室・11㎡`。显示在「広さ・仕様」那一行 |
| 定員 | 人数（数字） |
| ベッドサイズ（例: W1000×L1950） | 留空就不显示这一行 |
| 階数（例: 8F～13F） | 留空就不显示这一行 |
| 画像ID | 点「画像を選ぶ」，没照片就留空 |

下方 **「＋ 追加」** 加一个房型。

### 施設情報 的表格（行可以自由增减）

同一页里的 **「施設情報の表（行の追加・並べ替え・削除ができます）」**，每一行两栏：

| 栏 | 填什么 |
|---|---|
| 項目名 | 左边的标题。例：客室数、開業、チェックイン、電話、FAX |
| 内容 | 右边的内容。可以直接写文字，也可以用下面的占位符 |

**占位符**（写在「内容」里，生成页面时自动替换成上面已设定的值，不用重复维护）：

| 写法 | 会变成 |
|---|---|
| `{hotelname}` | 酒店名（各语言自动对应） |
| `{rooms}` | 客室数（例：36 室） |
| `{opened}` | 開業（運営中 / 2026年春 予定 等） |
| `{checkin}` | チェックイン時間 |
| `{checkout}` | チェックアウト時間 |
| `{address}` | 住所 |
| `{tel}` | 電話番号 |

现在每家酒店默认是 5 行（客室数 / 開業 / チェックイン / チェックアウト / 住所，全部用占位符）。
想加「電話」就点 **「＋ 追加」**，項目名填 `電話`、内容填 `{tel}`；
想加「FAX」就項目名填 `FAX`、内容直接写号码。↑↓ 可以调顺序，× 删除。

> 項目名会自动翻成其他 4 种语言。内容里如果只有数字和符号（电话号码等）会原样输出，不翻译。

卡片上最多显示 **4 行**：広さ・仕様 / 定員 / ベッドサイズ / 階数。**留空的行不会出现**，
所以只填两行的酒店看起来和以前一样。

> **这 4 行左边的标题文字**（広さ・仕様、定員、ベッドサイズ、階数）是全酒店共用的，
> 在左侧菜单 **「ホテル詳細ページの見出し」** 组里改，5 种语言分别维护。
> ベッドサイズ 和 階数 的内容本身（`W1000×L1950`、`8F～13F`）原样输出，不做翻译。

### ギャラリー写真

每张照片一张卡片：

| 栏 | 填什么 |
|---|---|
| 画像ID | 点「画像を選ぶ」 |
| キャプション | **下拉选择**：外観 / ロビー / フロント / 客室 / バスルーム / シャワールーム / ロッカー / 洗面スペース / コインランドリー / ダイニング / アメニティ / キャビン |

建议 **4〜6 张**。全部删掉就回到占位图。

> 想增加新的キャプション种类，要在「共通・ナビ・フッター」组里改 `photo_labels`，或找开发者。

### 客室設備・館内設備 / 備品・アメニティ

两个多行列表，**按你排的顺序显示**。图标由系统按关键词自动匹配。

建议的统一顺序：

```
全室無料Wi-Fi → 24時間フロント → 全室禁煙 → 液晶テレビ
→〔冷蔵庫（有则加）〕→ 電気ケトル → 洗浄機能つきトイレ
→〔コインランドリー（有料）/ 大型ロッカー / 共用シャワー・バスルーム（有则加）〕
→ 各店特有项目（朝食無料・カフェ・ズボンプレッサー 等）
```

備品・アメニティ 7 家店目前统一为：スリッパ・ドライヤー・タオル・シャンプー・コンディショナー・ボディシャンプー・館内着。

新宿歌舞伎町的 Cabin 店是舱型，只写实际有的：全館無料Wi-Fi・24時間フロント・全館禁煙・大型ロッカー・共用シャワー・バスルーム。

> 写了系统认不出的新词，会显示一个通用图标。要加专属图标请找开发者（见第 26 节）。

### 予約サイトリンク（OTA）

每条一张卡片：**サイト名**（例：楽天トラベル）+ **URL**。
显示在详情页右侧。接了 予約プロ 的酒店，右侧显示搜索表单而不是链接列表。

---

## 7-2. 给某家酒店接入 予約プロ 在线预约（重要）

以前只有樟葉店有「填日期 → 直接查空房」的表单。**现在任何一家酒店签了 予約プロ 之后，
你自己在后台点几下就能开通，不需要找开发者。**

打开那家酒店的编辑页，找到 **「オンライン予約フォーム（予約プロ）」**：

| 栏 | 怎么填 |
|---|---|
| **予約フォームの表示** | `使わない` = 只显示预约网站链接（默认）；`予約プロを使う` = 页面内显示预约表单 |
| **施設ID または 予約プロの URL** | 予約プロ签约后发的**数字施设ID**（例：`27000054`）。**也可以直接把予約プロ管理画面的网址整条粘贴进去**，系统会自动读出ID |
| **選べる人数の上限（名）** | 表单里「人数」下拉最多能选到几名。默认 5 |
| **選べる泊数の上限（泊）** | 同上，最多几泊。默认 5 |
| **選べる部屋数の上限（室）** | 同上，最多几间。默认 5，最大 10。这个上限同时作用于**首页搜索的「部屋数」**和该店详情页的预约表单 |

填完 ID 后，下面会立刻出现 **「確認：実際に使われる予約先」**，把两条真实网址显示出来：

```
検索ボタン　　　　　→ https://www7.489pro.com/asp/489/menu.asp?id=27000054&ty=ser
「予約サイトを開く」→ https://www7.489pro.com/asp/489/menu.asp?id=27000054
```

**保存前请点开这两条确认一下**，能正常打开那家店的予約プロ页面就没问题。
ID 填错的话这里会显示红字提示，不会生成错误链接。

保存后：

- 那家酒店 5 个语言的详情页右侧都会变成搜索表单
- **首页搜索框下方那句「このサイトから直接オンライン予約できるホテル：…」会自动把这家店加进去**，
  不需要另外改文案
- 想停用，把「予約フォームの表示」改回 `使わない` 即可，ID 会保留

> 予約プロ 以外的预约引擎目前不支持。要接别的引擎请找开发者。

---

## 8. 增加 / 删除 / 排序酒店

左侧「ホテル」下面第一项 **「追加・並び替え・削除」**。

### 调整顺序

每行右边 `↑ ↓`。**这个顺序就是首页酒店一览、页脚酒店列表和左侧菜单的顺序。**

### 加一家新酒店

填三样，然后点「このホテルを追加」：

| 栏 | 说明 |
|---|---|
| **ホテル名（日本語）** | 例：`ELE Hotel 渋谷イースト` |
| **URL用の名前** | 半角小写字母 / 数字 / 连字符，3 字以上。例：`shibuya-east` → 网址 `/hotels/shibuya-east.html`。**公开后就不该再改**，请一次想好 |
| **エリア** | 从下拉里选。选项来自「トップページ」的「エリア（絞り込みボタン）」，可以自己增加（见第 8-2 节） |

输入时会实时检查长度、非法字符、和已有酒店重名，有问题会标红并禁用按钮，下方还会预览最终网址。

加完会**自动跳到那家酒店的编辑页**，公開状態默认是「開業予定」。接下来：

1. 在日本語页签把资料填上（キャッチコピー、住所、アクセス、設備、房型…）
2. 上传照片并用「画像を選ぶ」接上
3. 保存 → 5 个语言的页面自动生成，自动出现在首页一览、地区筛选、页脚
4. 开业后把公開状態改成「公開中」

> 没填的栏显示「準備中」，所以**资料不全也可以先建页面**，慢慢补。

### 删除一家酒店

点那一行的 **「削除」**，弹窗要你**手打一遍它的 URL 用名**才能确定 —— 这是防误删。

保存后 5 个语言的详情页都会消失，一览和页脚里也会移除。**照片本身会留着，不会被删。**

> **删除不可撤销。** 只是暂时不想显示的话，把公開状態改成「開業予定」，或者找开发者处理，都比删除安全。

### 变更清单里怎么显示

调顺序、增、删在「変更内容を確認」里各自只有一条：

```
ホテルの並び順　　銀座イースト → 東上野 → …　　⟶　東上野 → 銀座イースト → …
ホテルを追加　　　（なし）　　⟶　ELE Hotel 渋谷イースト（/hotels/shibuya-east）
ホテルを削除　　　ELE Hotel ○○（/hotels/xxx）　　⟶　（なし）
```

---

## 8-5. 改顶部菜单 / 页脚链接

左侧菜单点 **「メニュー（ヘッダー・フッター）」**，里面有两份互相独立的列表：

| 字段 | 对应位置 |
|---|---|
| ヘッダーメニュー（画面上部） | 电脑版右上、手机版汉堡菜单里的那排链接 |
| フッターのリンク（インフォメーション欄） | 页脚右侧「インフォメーション」那一列 |

每个项目有两栏：

- **表示名** —— 显示出来的文字（写日文，其他 4 种语言保存时自动翻译）
- **リンク先** —— 跳到哪里

### リンク先能写什么

| 写法 | 效果 |
|---|---|
| `#hotels` `#brand` `#rooms` `#news` `#faq` | 跳到首页对应区块 |
| `/` | 首页 |
| `/contact` | 联系页 |
| `/privacy` | 隐私政策 |
| `/grand` | Grand ELE 专页 |
| `/hotels/kuzuha` | 某家酒店的详情页（换成那家店的 URL 用名字） |
| `/pages/recruit` | 后台自己新建的页面（见第 8-6 节） |
| `https://…` | 外部网站。**自动在新标签页打开**，文字后面会自动加一个小箭头图标 |

> 注意：写 `/hotels/…`、`/pages/…` 时**不要**加 `.html`，也不要加 `https://ele-hotel.com`。
> 系统会自动换成各语言版本的正确地址（例如英文版会自动指向 `/en/…`）。

### 增 / 删 / 排序

- 卡片右上角 **↑ ↓** 调顺序、**×** 删除
- 列表最下方 **「＋ 追加」** 加一项
- 项目的增删和排序**只能在「日本語」标签下操作**；切到其他语言标签时，只能校对译文

### 现在页脚里的「採用情報」

已经加好了，リンク先 是 `https://en-gage.net/tej_saiyo/`，点了会开新窗口。
以后招聘页换地址，只要改这一栏就行。

---

## 8-6. 自由新增一个页面

想做「会社案内」「利用規約」「特定商取引法に基づく表記」这类页面时用这个功能。

### 新建

1. 左侧 **ページ → 追加・並び替え・削除**
2. 填两栏：
   - **ページタイトル（日本語）** —— 例：`会社案内`
   - **URL用の名前（半角英数字）** —— 例：`about-us`（小写英文、数字、连字符，3 个字以上）
3. 点「このページを追加」，会自动跳到这一页的编辑画面

公开后的地址是 `/pages/about-us.html`，英文版是 `/en/pages/about-us.html`，5 种语言全部自动生成。

### 编辑内容

| 字段 | 说明 |
|---|---|
| URL用の名前 | 只读。建好之后不能改；要换名字就删掉重建 |
| ページタイトル | 页面大标题，也是面包屑里显示的名字 |
| 小見出し | 标题上方那行小字（英文，例：`ABOUT US`）。留空就不显示 |
| リード文 | 标题下面的一段引导文字。留空就不显示 |
| 本文ブロック | 正文。可以加任意多块，每块有「見出し」和「本文」 |
| 検索結果に出るタイトル / 説明文 | SEO 用。留空会自动用页面标题和リード文 |

**本文的写法**：

- 空一行 = 分成新的一段
- 每行开头写 `・`（或 `-`）= 变成项目符号列表
- 不能写 HTML 标签（会原样显示出来，这是为了安全）

### 放到菜单里

新建的页面**默认不会出现在菜单里**。想让访客能点到，去第 8-5 节的
「メニュー（ヘッダー・フッター）」加一项，リンク先 填 `/pages/about-us` 即可。

### 排序和删除

在「ページ → 追加・並び替え・削除」里用 ↑ ↓ 调顺序（只影响左侧菜单的排列）、
「削除」删除。删除要输入那个 URL 用的名字来确认，删了之后 5 种语言的页面都会消失，
**记得同时把菜单里指向它的那一项也删掉**。

### 翻译

页面内容和其他文字一样，保存时自动翻译成简体中文 / 繁体中文 / 英文 / 韩文。
想校对，在这一页上方切换语言标签直接改。

---

## 8-7. お問い合わせ的收件人设定

访客在联系表单里选的「お問い合わせ種別」，决定这封邮件发给谁。

### 现在的分派

| お問い合わせ種別 | 收件人 |
|---|---|
| ご予約・空室について | **访客选的那家酒店**的窗口邮箱 |
| ご宿泊中のご相談 | 同上 |
| 忘れ物について | 同上 |
| 法人・団体でのご利用 | sales@ele-hotel.com |
| 遊休不動産の活用・ホテル運営受託について | sales@ele-hotel.com |
| 採用について | hr@tej.jp |
| その他 | info@ele-hotel.com |

选了上面前 3 项时，表单上会**自动出现「エリア」和「ホテル」两个下拉框**，ホテル 变成必填。
选エリア会把ホテル列表筛掉不相关的；某个地区只有一家店时会自动选中。
选其他类别时这两个框会自动隐藏。

### 改分派规则

左侧菜单 **「お問い合わせページ」** → **「お問い合わせ種別」** 列表。每一项有两栏：

| 栏 | 填什么 |
|---|---|
| 表示名（お問い合わせ種別） | 下拉里显示的文字。改了会自动翻成其他 4 种语言 |
| 送信先メールアドレス | 填 `hotel` = 发给访客选的那家酒店；填具体邮箱（例 `sales@ele-hotel.com`）= 固定发到这个地址 |

项目本身也能增加、改顺序、删除。**留空**或写错的，会退回默认的 info@ele-hotel.com。

### 改某家酒店的收件邮箱

打开那家酒店的编辑页，找 **「お問い合わせの受信メールアドレス（このホテル宛）」**。
新开一家酒店时在这里填上就行，不用找开发者。

目前的设定：

| 酒店 | 收件邮箱 |
|---|---|
| 銀座イースト | rsv1001@ele-hotel.com |
| 新宿歌舞伎町 | rsv1002@ele-hotel.com |
| 樟葉 | rsv1003@ele-hotel.com |
| 東上野 | rsv1004@ele-hotel.com |
| 東日本橋 | rsv1005@ele-hotel.com |
| 仙台東口 | rsv1006@ele-hotel.com |
| 名古屋栄 | rsv1007@ele-hotel.com |

> 收到的邮件正文里会带一行「対象ホテル」，方便确认。
> 收件人是**服务器端**根据类别和酒店重新算出来的，不是从浏览器传过去的，所以改不了、也不会被滥用。

---

## 8-2. 增加一个新的地区（エリア）

地区筛选按钮（すべて / 東京エリア / 大阪エリア…）的清单在
**「トップページ」→「エリア（絞り込みボタン）」**，每个地区一张卡片：

| 栏 | 说明 |
|---|---|
| **英数字キー** | 半角小写英文，例 `fukuoka`。**保存之后不要再改** —— 已经归到这个地区的酒店会失去关联 |
| **表示名** | 日文显示名，例 `福岡エリア`。其他 4 个语言保存时自动翻译 |
| **英語表記** | 首页主视觉上方那行 `TOKYO / NAGOYA / …` 用的英文，例 `Fukuoka` |

加完保存，之后在酒店编辑页的「エリア」下拉里就能选到它。

> **筛选按钮只会显示"至少有一家公開中酒店"的地区**，所以先加地区、按钮不出现是正常的，
> 等把酒店归进去就会出现。

---

## 8-3. 换首页主视觉 / 品牌区块的照片

在 **「トップページ」** 里：

- **トップ大画像の画像ID** —— 首页最上方那张大图
- **ブランド紹介の写真の画像ID** —— 品牌介绍区块的照片

在 **「GRAND ELE ページ」** 里，7 张照片各有一个「…の画像ID」字段。

用法和第 6 节一样，点「画像を選ぶ」挑就行。留空会回到系统默认图。

---

## 8-4. 单独设置某家酒店的 SEO

酒店编辑页里有两栏：

| 栏 | 说明 |
|---|---|
| **検索結果に出るページタイトル（SEO・空欄なら自動）** | 留空 = 自动用「酒店名｜ELE HOTEL」 |
| **検索結果に出るページ説明文（SEO・空欄なら自動）** | 留空 = 自动用那家店的キャッチコピー |

**平时留空就好**，只有想针对搜索引擎特别写一段时才填。填了会自动翻译成 5 个语言。

---

## 9. 保存与发布

### 流程

```
改内容 → 「変更内容を確認」 → 核对对照表 → 「保存して公開」 → 约 1 分钟后上线
```

**后台没有「草稿」概念 —— 点了保存就是直接发布到线上。**

这 1 分钟里系统在做：

1. 把改动记录到 GitHub（等于自动存了一个版本，随时可以回溯）
2. 用 AI 把新写的日文翻成中英韩
3. 重新生成 5 语言 × 11 页 = 55 个页面
4. 部署上线

### 保存栏上的数字

「未保存 12件」= 有 12 处改动没保存。包含文字改动和译文改动。

### 几种会遇到的提示

| 提示 | 意思 | 怎么办 |
|---|---|---|
| **ほかの方が先に保存しました。画面を再読み込みしてください。** | 你打开页面之后，别人保存过一次 | 按提示重新载入再改。你手上的改动会丢，所以**多人同时改同一处要先打个招呼** |
| 保存后网站没变 | 大概率是浏览器缓存 | 在网站页面按 `Ctrl+Shift+R`（Mac 是 `Cmd+Shift+R`） |
| **翻訳に失敗しました（…）** | AI 翻译当天用量到上限，或临时故障 | 翻不出来的语言会**暂时显示日文**，不会空白。过一会儿随便改点什么再保存一次，会重新尝试 |

### 后台界面本身看起来是旧的

按 `Ctrl+Shift+R` 强制刷新。后台文件本身已经设成不缓存，但极少数情况下浏览器还是会留旧版本。

---

## 10. 常见问题

**Q. 改错了想撤销？**
保存之前：刷新页面就回到上次保存的状态（未保存的改动会丢）。
保存之后：所有版本都记录在 GitHub 里，找开发者可以回退到任意一次保存。

**Q. 能不能先做个草稿给上司看，同意了再发布？**
现在不行，保存即发布。需要的话可以另外开发「预览模式」。

**Q. 首页那个搜索框能搜到房间吗？**
目前是引导性的，会提示去樟葉店页面预约。只有樟葉店接了预约引擎（見第 24 节），其他店走 OTA 链接。今后各店都接入后可以改成直连。

**Q. 手机上能用后台吗？**
能，界面会自适应。但上传照片和长文编辑建议用电脑。

**Q. Logo 能改吗？**
**图形已注册商标，不要改动图形本身。** 只能改颜色，而且要找开发者改样式文件。

**Q. 密码要怎么改？**
见第 12 节，管理者在 Cloudflare 上改。

**Q. 想加一个语言（比如泰语）？**
框架已经预留，见第 21 节，需要开发者操作。

---

# 第二部　管理与运维

## 11. 系统构成

```
              ┌─────────────────────────────────────────────┐
              │  管理后台  https://ele-hotel.com/admin       │
              │  （邮箱白名单 + 密码，12 小时会话）           │
              └────────────────┬────────────────────────────┘
                               │ 「保存して公開」
                               ▼
              ┌─────────────────────────────────────────────┐
              │  ① Cloudflare Workers AI 自动翻译            │
              │     日文 → 简中 / English / 한국어            │
              └────────────────┬────────────────────────────┘
                               ▼
              ┌─────────────────────────────────────────────┐
              │  ② GitHub 私有仓库 avexe816/ele-hotel-site   │
              │     每次保存 = 一次 commit（自动版本记录）     │
              └────────────────┬────────────────────────────┘
                               ▼
              ┌─────────────────────────────────────────────┐
              │  ③ GitHub Actions（deploy.yml）              │
              │     OpenCC 生成繁體 → python3 build.py       │
              │     → wrangler pages deploy                 │
              └────────────────┬────────────────────────────┘
                               ▼
              ┌─────────────────────────────────────────────┐
              │  ④ Cloudflare Pages 项目「ele-hotel」        │
              │     ele-hotel.com / www.ele-hotel.com       │
              └─────────────────────────────────────────────┘
```

整个链路约 **55 秒〜1 分半**。

### 关键账号 / 位置一览

| 项目 | 值 |
|---|---|
| Cloudflare 账号 | **TEJ**（Account ID `0abcaf3ee0a5d1804975ce92d33255ca`） |
| Cloudflare Pages 项目 | **ele-hotel** |
| 自有域名 | `ele-hotel.com` + `www.ele-hotel.com`（CNAME → `ele-hotel.pages.dev`，已开启代理） |
| GitHub 仓库 | **https://github.com/avexe816/ele-hotel-site**（私有，分支 `main`） |
| 自动部署工作流 | 仓库 → Actions → **Build & Deploy** |
| 收件邮箱 | `info@ele-hotel.com`（表单收件） |
| 发件邮箱 | `noreply@ele-hotel.com`（Lark SMTP） |
| 公司邮箱托管 | **Lark（飞书）** —— MX 指向 larksuite.com |
| DNS 权威服务器 | Xserver（`ns1/ns2/ns3.xdomain.ne.jp`） |

---

## 12. 谁能登录（增删可登录邮箱 / 改密码）

两个都在 **Cloudflare Dashboard → Workers & Pages → `ele-hotel` → Settings → Variables and Secrets**。

| 变量名 | 作用 |
|---|---|
| `ADMIN_EMAILS` | **可以登录的邮箱白名单**。多个用**逗号**分隔，例：`ukh816@gmail.com, tanaka@ele-hotel.com` |
| `ADMIN_PASSWORD` | 全员共用的登录密码（请设为 Secret / 加密类型） |

### 加一个人

1. 打开上面那个页面，编辑 `ADMIN_EMAILS`，在末尾加 `, 新邮箱`
2. 保存
3. **必须手动重新部署一次才生效** —— 见第 14 节

### 踢掉一个人

从 `ADMIN_EMAILS` 里删掉那个邮箱 → 保存 → 重新部署。
生效后那个人**当场**就用不了了（每次请求都会重新核对白名单），不用等会话过期。

### 改密码

改 `ADMIN_PASSWORD` → 保存 → 重新部署。已登录的人在 12 小时内不受影响。

> **`ADMIN_EMAILS` 留空时**，系统会退回到只允许 `ukh816@gmail.com`（防止误清空导致全员被锁在外面）。
>
> 员工离职、密码疑似泄露时，**改密码 + 删邮箱两件一起做**。

---

## 13. Cloudflare 环境变量一览

| 变量名 | 类型 | 作用 | 改动后 |
|---|---|---|---|
| `ADMIN_EMAILS` | 普通 | 后台登录白名单 | 需重新部署 |
| `ADMIN_PASSWORD` | Secret | 后台登录密码 | 需重新部署 |
| `ADMIN_SECRET` | Secret | 诊断接口的口令（见第 16 节） | 需重新部署 |
| `GH_TOKEN` | Secret | 后台写回 GitHub 用的令牌（细粒度 PAT，只授权本仓库） | 需重新部署 |
| `AI` | **绑定** | Workers AI 绑定，自动翻译用。这不是变量，要在 **Bindings** 里加 | 需重新部署 |
| `SMTP_HOST` | 普通 | `smtp.larksuite.com` | 需重新部署 |
| `SMTP_PORT` | 普通 | `465` | 需重新部署 |
| `SMTP_USER` | 普通 | `noreply@ele-hotel.com` | 需重新部署 |
| `SMTP_PASS` | Secret | Lark 的 IMAP/SMTP 专用密码 | 需重新部署 |

GitHub 侧还有两个 Secrets（仓库 → Settings → Secrets and variables → Actions）：

| 名称 | 作用 |
|---|---|
| `CLOUDFLARE_ACCOUNT_ID` | 部署目标账号 |
| `CLOUDFLARE_API_TOKEN` | 部署用令牌（需要 Pages 编辑权限） |

> **`GH_TOKEN` 会过期。** 细粒度 PAT 默认有有效期，到期后后台保存会失败（提示写入 GitHub 出错）。
> 到期前去 GitHub → Settings → Developer settings → Personal access tokens 重新生成，
> 记得**授权范围要包含 `avexe816/ele-hotel-site` 仓库的 Contents 读写**，然后更新 Cloudflare 上的 `GH_TOKEN` 并重新部署。

> **Workers AI 免费额度**：每天 10,000 neurons。正常运营（一天改十几处）完全够。
> 一次性大改（比如新增一家酒店的全部资料）可能撞到上限，那就分两天保存，或者当天晚点再补一次保存。

---

## 14. 手动重新部署

**什么时候需要**：改了 Cloudflare 环境变量 / 绑定之后。（在后台保存内容**不需要**，那是自动的。）

1. 打开 **https://github.com/avexe816/ele-hotel-site/actions**
2. 左侧选 **Build & Deploy**
3. 右上 **Run workflow** → 分支选 `main` → 点绿色的 **Run workflow**
4. 等约 1 分钟，出现绿色对勾即完成

> **不要**用 Cloudflare Pages 页面上的 Retry deployment —— 那会重放旧的构建产物，绕过最新的源码。
> 统一从 GitHub Actions 触发。

---

## 15. 绝对不要碰的东西

| 不要碰 | 原因 |
|---|---|
| **DNS 的 NS 记录** | 权威服务器在 Xserver，改了整个域名会失效 |
| **DNS 的 MX 记录** | 公司邮箱跑在 Lark 上，改了**全公司收不到邮件** |
| **DNS 的 TXT / SPF 记录** | 同上，改了公司发出去的邮件会被判垃圾邮件 |
| **Logo 的图形** | 已注册商标。只能改颜色 |
| 用 Cloudflare API 去 PATCH `deployment_configs` | 会把读不出来的 Secret 全部清空 |
| 手改仓库里生成出来的 HTML | 下次构建会被覆盖 |

需要在 Cloudflare 侧加自有域名或改代理设置时，**只动 CNAME，不动 NS / MX / TXT。**

---

## 16. 诊断接口

出问题时先看这三个。前两个需要口令 —— 值是 Cloudflare 上的 `ADMIN_SECRET`。

| 地址 | 作用 | 需要口令 |
|---|---|---|
| `https://ele-hotel.com/api/admin/me` | 一眼看出各项配置齐不齐：`hasPassword` / `emailsConfigured` / `translator` / `repo` 等 | 不需要 |
| `https://ele-hotel.com/api/admin/diag?secret=<口令>` | GitHub 令牌是否有效、能不能读到仓库和各个数据文件 | 需要 |
| `https://ele-hotel.com/api/admin/translate-test?secret=<口令>` | 用 3 个例句测 AI 翻译是否正常 | 需要 |

`/api/admin/me` 全是 `true` 就说明环境变量都配好了。某项是 `false`，就去第 13 节的表里找对应的变量。

实时日志：**Cloudflare Dashboard → Workers & Pages → `ele-hotel` → Logs**。

---

## 17. 出问题时的排查顺序

### 后台登录不上

1. 邮箱是不是在 `ADMIN_EMAILS` 里（注意有没有多余空格、拼错）
2. 改过环境变量后**有没有重新部署**（第 14 节）
3. 打开 `/api/admin/me`，看 `hasPassword` 和 `emailsConfigured` 是不是都 `true`

### 后台能进，但保存失败

1. 打开 `/api/admin/diag?secret=…`，看 GitHub 令牌部分
2. 最常见原因：**`GH_TOKEN` 过期，或者令牌没授权到这个仓库**（第 13 节）
3. 提示「ほかの方が先に保存しました」→ 别人同时在改，重新载入

### 保存成功但网站没更新

1. 先按 `Ctrl+Shift+R` 排除缓存
2. 去 GitHub Actions 看最新一次运行是不是绿的。红的话点进去看哪一步失败
3. 都正常的话，去 Cloudflare Pages → ele-hotel → Deployments 确认有新的部署记录

### 译文一直是日文

1. Cloudflare 上 **`AI` 绑定**是不是加了（这是 Bindings，不是 Variables）
2. `/api/admin/translate-test?secret=…` 能不能返回正常译文
3. 当天 Workers AI 额度是不是用完了 → 隔天再保存一次

### 联系表单发不出邮件

见第 25 节的排查表。

### 后台界面缺功能 / 长得不对

`Ctrl+Shift+R`。后台文件已设为不缓存，且资源链接带版本号，但浏览器偶尔仍会留旧版本。

---

# 第三部　开发者附录

第一部和第二部覆盖了日常运营的全部场景。这一部分是给要改设计、加语言、改页面结构的人看的。

## 18. 环境准备与目录结构

```bash
git clone https://github.com/avexe816/ele-hotel-site.git
cd ele-hotel-site
pip install pillow opencc-python-reimplemented
python3 build.py
```

构建成功会打印：

```
built 55 pages (ja, en, zh, zh-Hant, ko)
built _worker.js (42143 bytes)
stamped admin/index.html (cache busting)
translations complete
```

```
ele-hotel-site/
├─ build.py                生成器：读 data/*.json → 输出 55 个 HTML
├─ amenity_icons.py        设施 / アメニティ 的 SVG 图标与关键词匹配表
├─ import_photos.py        批量导入照片的示例脚本
├─ _headers                Cloudflare Pages 的响应头（/admin/* 设为 no-store + noindex）
├─ data/
│  ├─ site.json            全站文字（日文）：导航、首页、FAQ、公告、联系、隐私政策、共用标签
│  ├─ hotels.json          7 家酒店的全部资料（日文）
│  ├─ grand.json           Grand ELE Hotel 专页文案（日文）
│  ├─ i18n.json            翻译库（约 500 条）：日文 → en / zh / zh-Hant / ko / th
│  └─ admin-schema.json    后台界面的定义：分组、字段名、字段类型
├─ admin/
│  ├─ index.html           后台外壳（资源链接由 build.py 打上内容哈希）
│  ├─ app.js               后台全部前端逻辑（无框架，纯 DOM）
│  └─ style.css            后台样式
├─ src/                    Worker 源码，由 tools/build_worker.py 按顺序拼成 _worker.js
│  ├─ admin_translate.js   自动翻译（Workers AI）
│  ├─ admin_api.js         /api/admin/* 全部接口：登录、读取、保存、上传、诊断
│  ├─ worker_contact.js    /api/contact 表单发信
│  └─ worker_main.js       路由入口
├─ assets/
│  ├─ style.css            公开站样式（配色、字体在最上方的 :root 变量区）
│  ├─ favicon.svg
│  └─ img/                 全部图片（136 个 webp，含 -sm 手机版）
├─ tools/
│  ├─ build_worker.py      拼接 src/*.js → _worker.js
│  ├─ i18n.py              翻译库读写、key 计算
│  ├─ i18n_sync.py         翻译库命令行工具
│  ├─ import_image.py      单张图片 → webp 两档
│  ├─ zh_hant.py           OpenCC 简→繁 + 港台用词修正
│  ├─ migrate_i18n.py      历史迁移脚本（已用完）
│  ├─ cf_deploy.py         Direct Upload 部署脚本（现已由 Actions 代替）
│  └─ smtp_test.mjs        SMTP 连通性测试
├─ .github/workflows/deploy.yml   自动部署
├─ index.html / grand.html / privacy.html / contact.html / hotels/*.html   日文版（生成物）
├─ en/ · zh/ · zh-hant/ · ko/     其他语言（生成物）
└─ 运维手册.md
```

`index.html`、`hotels/`、`en/`、`zh/`、`zh-hant/`、`ko/`、`_worker.js` 都是**自动生成**的，不要手改。
`_worker.js` 已在 `.gitignore` 里，由 CI 构建时生成。

---

## 19. 本地改数据

日常内容改动请用后台。本地改的场合（批量数据迁移、改结构等）：

```bash
# 改 data/*.json（只写日文）
python3 -c "import json;json.load(open('data/hotels.json'));print('OK')"   # 语法检查
python3 build.py
git add -A && git commit -m "..." && git push origin main                   # push 即自动部署
```

**JSON 注意点**：引号是半角 `"`，项之间有逗号，最后一项后面**不能**有逗号。

### `data/hotels.json` 的字段

| 字段 | 说明 |
|---|---|
| `slug` | 网址名，`/hotels/<slug>.html`。小写字母 / 数字 / 连字符 |
| `area` | `tokyo` / `osaka` / `nagoya` / `sendai`（新增地区要在 `site.json` 里加 `filter_<地区>` 标签） |
| `status` | `open`（公開中）/ `soon`（開業予定） |
| `rooms` | 客房总数（数字）。为空或 0 时页面显示「準備中」 |
| `brand` | `hotel` / `apart` / `onsen` / `grand` |
| `img` | 主图的图片ID（不带 `.webp`） |
| `name` `tagline` `address` `opened` | i18n 字段，写 `{"ja": "…"}` |
| `access` `nearby` `roomtypes` `facilities` `amenities` | i18n 数组，写 `{"ja": ["…", "…"]}` |
| `rtypes` | 房型卡片数组，每项 `{name, detail, cap, img}`，`name`/`detail` 是 i18n |
| `gallery` | 画廊数组，每项 `{img, label}`，`label` 是 `photo_labels` 里的**代码**（`ext`/`lobby`/`room`…） |
| `ota` | 预约链接数组，每项 `{label, url}`（`label` 是 i18n） |
| `map_q` | Google 地图搜索词，空格用 `+` |
| `tel` `checkin` `checkout` `price_from` | 非 i18n 的纯文本 |
| `booking` | 予約プロ 配置（只有 kuzuha 有，见第 24 节） |

`gallery` 的 `label` 代码 → 显示文字的对照表在 `data/site.json` 的 **`photo_labels`**（12 项：
`ext` 外観 / `lobby` ロビー / `desk` フロント / `room` 客室 / `bath` バスルーム / `shower` シャワールーム /
`locker` ロッカー / `wash` 洗面スペース / `laundry` コインランドリー / `dining` ダイニング /
`amenity` アメニティ / `cabin` キャビン）。后台的下拉选项就是读这里。

### 空值的处理

`build.py` 里所有 fact 行、房间数、房型规格在值为空时都会输出 `site.json` 的 `tbd`（=「準備中」）。
`status: "soon"` 的酒店，开业提示优先用它自己的 `opened`，为空时才用 `hotel_soon_note`。

---

## 20. 图片规格与批量导入

| 变体 | 输出文件 | 最长边 | 质量 |
|---|---|---|---|
| 电脑用 | `assets/img/<id>.webp` | 1800px | 0.80 |
| 手机用 | `assets/img/<id>-sm.webp` | 900px | 0.76 |

**后台上传走的是浏览器端 canvas，参数与 `tools/import_image.py` 完全一致**，两条路产出的文件可以互换。

```bash
python3 tools/import_image.py ~/照片/銀座外観.jpg p-ginza-east-hero
python3 build.py
```

批量时照抄 `import_photos.py`，改一下文件夹路径。

### 命名规则

- 图片ID 正则：`^[a-z0-9][a-z0-9-]{0,58}[a-z0-9]$`
- 单张上传上限 **4 MB（转换后）**，一次最多 **20 张**
- 真实照片统一 `p-<slug>-<用途>`；旧的形象图是 `h-<slug>` / `hero-lobby` / `brand-*` / `grand-*` 等

### 主要固定名称

| 用途 | 文件名 |
|---|---|
| 首页主视觉 | `hero-lobby` |
| 品牌系列卡片 | `brand-street`（Hotel）/ `brand-apart` / `brand-onsen` / `brand-grand` |
| 客室・設備区块 | `room-double` / `room-twin` / `breakfast` / `amenity-cafe` / `amenity-detail` |
| Grand 专页 | `grand-hero` / `grand-suite` / `grand-bath` / `grand-dining` / `grand-lounge` / `grand-facade` / `grand-detail` |

图片全部换成真实照片后，记得删掉「本页图片为品牌形象图」的注释：Grand 页在 `data/grand.json` 的
`img_note`，酒店详情页在 `data/site.json` 的 `gallery_note` / `rooms_note` / `amenity_note` / `photo_soon`。

---

## 21. 多语言机制

### 原理

`data/` 里只有日文。`build.py` 拿每一句日文去 `data/i18n.json` 查译文；查到用译文，查不到先显示日文并在终端提示差几条。

```
data/site.json（日文）  ──┐
data/hotels.json（日文）──┼──►  build.py  ──►  ja / en / zh / zh-Hant / ko 页面
data/grand.json（日文） ──┘         ▲
                          data/i18n.json（译文库）
```

翻译库一条长这样：

```json
"0537e672b005dab0": {
  "ja": "言語を選ぶ",
  "zh": "选择语言",
  "zh-Hant": "選擇語言",
  "en": "Choose language",
  "ko": "언어 선택",
  "th": "",
  "locked": true
}
```

- **key = 日文原文的 SHA-1 前 16 位**（`tools/i18n.py` 的 `key_of`）。
  同一句日文在不同位置需要不同译法时，会生成带路径的独立 key（`path_key`）。
- **`locked: true` = 人工确认过**，自动翻译不会覆盖。后台里改一句译文就是把它设成 `locked`。
- **改日文 = 换 key**，所以旧译文对不上了，会重新翻译。这是设计如此。

### 不要手改 `i18n.json` 的 `ja` 字段

那是 key 的来源，改了会丢译文。要改原文请改 `data/*.json`。

### 命令行工具

```bash
python3 tools/i18n_sync.py --stats                      # 各语言完成度
python3 tools/i18n_sync.py --todo en zh zh-Hant ko      # 导出待翻译清单
python3 tools/i18n_sync.py --import data/i18n-todo.json # 导入译文（自动备份 .bak，--force 覆盖已锁定）
python3 tools/i18n_sync.py --prune                      # 清掉不再使用的旧译文
```

### 繁體中文

不是重新翻译的，用 OpenCC `s2twp`（台湾正体 + 台港惯用词）从简体转换，再套一份人工用词修正表。

```bash
python3 tools/zh_hant.py                  # 由简体重算全部繁体
python3 tools/zh_hant.py --fill-missing   # 只补缺的（CI 里跑这个）
```

某条繁体要跟简体不同说法时，手改 `data/i18n.json` 那条的 `zh-Hant` 并加 `"hant_manual": true`，重算会跳过它。

### 增加泰语（或其他语言）

框架已预留，`data/site.json` 顶部 `langs` 里 th 是 `"enabled": false`。

1. `python3 tools/i18n_sync.py --todo th`，把 `data/i18n-todo.json` 的 `th` 填满（约 500 条）
2. `python3 tools/i18n_sync.py --import data/i18n-todo.json`
3. `site.json` 里 th 改成 `"enabled": true`
4. `python3 build.py` → 自动生成 `/th/` 全站，语言菜单多出「ไทย」，字体自动切 Noto Sans Thai
5. 后台要显示这个页签，还要在 `data/admin-schema.json` 的 `langs` 里加一条

---

## 22. 自动翻译的实现

`src/admin_translate.js`，模型是 Cloudflare Workers AI 的 **`@cf/qwen/qwen3-30b-a3b-fp8`**（temperature 0.2，
prompt 带 `/no_think`，输出里的 `<think>` 段会被剥掉）。

- 目标语言只有 **zh / en / ko** 三种。zh-Hant 由 CI 用 OpenCC 生成。
- prompt **必须是英文**，且**术语表（GLOSSARY）必须放在最前面** —— 放在末尾模型会忽略。
  术语表里有 7 家酒店的人工定名和约 20 个地名 / 车站名。
- 质量检查 `looksValid`：空、与原文完全相同、中文里出现假名、韩文里没有한글、英文里有汉字 → 判为失败。
- 三段重试：批量 12 条 → 逐条 `runSingle` → 兜底 `runM2M`（`@cf/meta/m2m100-1.2b`）。
  全失败的条目**保持 `null`，绝不写入**，页面回退显示日文。

### 保存的完整流程（`/api/admin/save`）

```
读取仓库当前 bundle
  → 比对 head，不一致返回 409（前端提示「ほかの方が先に保存しました」）
  → 套用请求里的手工译文（{key: {lang: text}}，值为 null 表示要求重新翻译）
  → fillTranslations() 补齐缺失译文
  → 一次 commit，包含 data/i18n.json
```

**一次保存 = 一次 commit**，不会产生中间状态。

---

## 23. 配色 / 字体

`assets/style.css` 最上方 `:root`：`--bg`、`--surface`、`--text`、`--primary`，
品牌色 `--b-hotel` / `--b-apart` / `--b-onsen` / `--b-grand`。深色模式在 `[data-theme="dark"]`。

后台样式在 `admin/style.css`，变量：`--bg #f7f6f3`、`--panel #fff`、`--text #1c1c1a`、
`--border #e3e1dc`、`--accent #8a6a4b`、`--danger #b4483c`、`--ok #3a7d4f`、`--side-w 240px`。

改完 `python3 build.py` —— 样式链接带内容哈希，会自动破缓存。

> **主题切换用的是 cookie `eletheme`**，不是 localStorage。
> 部署环境禁用了 localStorage / sessionStorage / indexedDB，**加代码时不要用这三个 API**。

---

## 24. 予約プロ 预约引擎（目前只有樟葉店）

配置在 `data/hotels.json` 里 kuzuha 的 `booking` 段：

```json
"booking": {
  "engine": "yoyakupro",
  "action": "https://www7.489pro.com/asp/489/menu.asp?id=27000054&ty=ser",
  "top":    "https://www7.489pro.com/asp/489/menu.asp?id=27000054",
  "max_guests": 5,
  "max_nights": 5,
  "max_rooms": 5
}
```

- `id=27000054` 是樟葉店的**施设 ID**。别的酒店签约后把这段整体复制过去，只改 `id`。
- 有 `booking` 的酒店，详情页右侧显示「空室検索・ご予約」表单（入住日 + 人数 + 泊数 + 部屋数），
  点搜索在**新窗口**打开予約プロ 的结果页；下面另有「公式サイトから予約する」直接进预约首页。
- 没有 `booking` 的酒店显示 OTA 链接列表。
- 予約プロ 的账号由酒店侧管理，**网站不保存任何账号信息**，也不需要 API key。

---

## 25. お問い合わせ 表单

### 页面

`/contact.html` 及 4 个语言版本。文案全在 `data/site.json` → `ja.contact_page`（后台的「お問い合わせページ」组）。
「お問い合わせ種別」的下拉选项 = `contact_page.kinds` 数组，每一项是 `{label, to}`：
`to` 写 `hotel` 表示发给访客选的那家酒店（取 `data/hotels.json` 的 `contact_email`），写邮箱则固定发到那里。
表单会把 `kind_i`（类别的序号，与语言无关）和 `hotel`（slug）一起送出，服务器端据此判断收件人。
浏览器端会先自检必填项、邮箱格式、隐私政策勾选，另有一个隐藏的防机器人字段。

### 发信

`src/worker_contact.js` 处理 `POST /api/contact`。收件人由 `resolveTo()` 按「種別 + 选中的酒店」决定
（对照表 `src/_contact_routes.js` 由 `build.py` 从 `data/site.json` 和 `data/hotels.json` 自动生成，
不要手改；运营侧的设定方法见第 8-7 节）。算不出来时退回 **info@ele-hotel.com**。
回信地址自动设为访客填的邮箱。按顺序挑第一个配齐的通道：

| 优先级 | 通道 | 需要的环境变量 |
|---|---|---|
| A（现用） | **公司 Lark 邮箱 SMTP** | `SMTP_HOST` `SMTP_PORT` `SMTP_USER` `SMTP_PASS` |
| B | Resend | `RESEND_API_KEY` |
| C | Brevo | `BREVO_API_KEY` |

可选：`CONTACT_TO`（默认 info@ele-hotel.com）、`CONTACT_FROM`（默认 `ELE HOTEL <SMTP_USER>`）。

Lark 公共邮箱的建立方式：Lark 管理后台 → 产品设置 → 邮箱 → 地址管理 → 公共邮箱 → 创建
`noreply@ele-hotel.com`，**务必勾「启用 IMAP/SMTP 服务」**，然后在「编辑」里取得 **IMAP/SMTP 专用密码**
（跟登录密码不是一回事，忘了可以重新生成）。

> **端口必须 465**（隐式 TLS）。587 的 STARTTLS 在 Cloudflare 运行时上有已知兼容问题；25 端口被 Cloudflare 封禁。
> 域名邮箱在 Lark 上，SPF / DKIM 本来就归 Lark 管，用 `@ele-hotel.com` 发信天然通过验证 —— **不用加 TXT，不用碰 MX。**
> 若改用 Resend / Brevo，只新增服务商指定的子记录，**绝不改动现有 MX 和 SPF(TXT)**。

### 排查

提交后应看到绿色的「送信が完了しました」，同时 info@ele-hotel.com 收到标题为「【ELE HOTEL お問い合わせ】…」的邮件。
失败时看 Cloudflare → `ele-hotel` → Logs 里 `contact mail failed:` 后面的原因：

| 日志 | 原因 |
|---|---|
| `smtp 535` | SMTP 用户名或专用密码不对 |
| `no_mail_transport_configured` | 四个 SMTP 变量没填全或拼错 |
| `smtp 550` / `553` | 发件地址跟 `SMTP_USER` 不一致，Lark 拒绝代发 |
| 连接超时 | 端口填了 25，或 `SMTP_HOST` 写错 |

---

## 26. 设施 / アメニティ 图标

`amenity_icons.py` 里有一张 `RULES` 表（关键词 → 图标名），从上往下取第一个命中的：

```python
RULES = [
    (("スリッパ",), "slipper"),
    (("ドライヤー",), "dryer"),
    (("コンディショナー", "リンス"), "conditioner"),
    ...
]
```

- 加新词又不想画新图标：把关键词加进已有图标那一行
- 要画新图标：在 `ICONS` 字典里加一个 24×24 视口的线性 SVG（`stroke` 用 `currentColor`，不写 `fill`），再在 `RULES` 加一行
- **顺序很重要**：更具体的词要排在更宽泛的词**前面**（「コンディショナー」必须在「シャンプー」之前，否则会被抢先命中）
- 改完 `python3 build.py`，打开一个详情页确认

---

## 27. プライバシーポリシー

- 页面：`privacy.html` 及 4 个语言版本（自动生成），入口在页脚「インフォメーション」栏最后一条
- 正文在 `data/site.json` 的 `privacy` 段（后台的「プライバシーポリシー」组）：
  `title`、`intro`、`sections[]`（每段 `h` 标题 + `p` 段落 + `list` 列表）、`contact_rows`（公司名 / 所在地 / 电话 / 受理时间）
- 署名主体 **株式会社TEJ**（〒104-0032 東京都中央区八丁堀3-18-9 第2直平ビル2階 / TEL 03-6260-8831）
- 共 10 条，结构参考了 hotel-monday.com 与 tej.jp。**今后委托律师审阅修改时，请只改后台 / `data/site.json`，不要改 HTML。**

---

## 附　7 家酒店现状

下表的顺序就是当前首页一览的显示顺序。

| slug | 名称 | 地区 | 状态 | 室数 | TEL | 開業 |
|---|---|---|---|---|---|---|
| `ginza-east` | ELE Hotel 銀座イースト | 東京 | 公開中 | 36 | 03-6869-6078 | 運営中 |
| `higashi-ueno` | ELE Hotel 東上野 | 東京 | 公開中 | 36 | 03-5830-6587 | 2021年12月開業 |
| `higashi-nihonbashi` | ELE Hotel 東日本橋 | 東京 | 公開中 | 30 | 03-6667-0262 | 2024年4月開業 |
| `shinjuku-cabin` | ELE Hotel Cabin 新宿歌舞伎町 | 東京 | 公開中 | 70 | 03-6233-9191 | 2023年6月開業（舱型） |
| `nagoya-sakae` | ELE Hotel 名古屋栄駅前 | 名古屋 | **開業予定** | 140 | 052-265-6216 | 2026年9月開業予定 |
| `kuzuha` | ELE Hotel 樟葉 | 大阪 | 公開中 | 72 | 072-856-7808 | 2023年12月 リブランド（**予約プロ 接入**） |
| `sendai-higashiguchi` | ELE Hotel 仙台東口 | 仙台 | 公開中 | 65 | 022-299-7521 | 2026年6月 全館リニューアルオープン |

---

*ELE HOTEL 官网 运维手册 v2.0*
