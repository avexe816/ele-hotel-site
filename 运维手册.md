# ELE HOTEL 官网 运维手册（v1.9）

网站是**静态站点**：所有内容写在 `data/` 目录的 JSON 文件里，运行一次 `python3 build.py`
就会生成 50 个 HTML 页面（5 种语言 × （首页 + Grand 页 + プライバシーポリシー + 7 家酒店详情页））。
不需要数据库、不需要后台，改完重新生成、重新部署即可。

> **v1.9 的变化**：① 详情页「設備」区块改名为「設備・アメニティ」，内部分成两组 —
> **客室設備・館内設備**（原来的设施图标）和 **備品・アメニティ**（新增的 7 项：スリッパ・
> ドライヤー・タオル・シャンプー・コンディショナー・ボディシャンプー・館内着）；
> ② 7 家酒店的设施图标统一成固定顺序（全室無料Wi-Fi → 24時間フロント → 全室禁煙 →
> 液晶テレビ →〔冷蔵庫〕→ 電気ケトル → 洗浄機能つきトイレ → 各店特有项目），
> 「冷蔵庫／液晶テレビ」这类合并写法全部拆成单独图标；③ 施設名统一为 **ELE Hotel 樟葉**
> （品牌单独出现时仍写 ELE HOTEL 全大写）；④ 删除首页最下方的 TEJ 品牌说明段落
> （页脚的「運営会社サイト」链接保留）；⑤ お問い合わせ 的咨询类别把「取材・広報について」
> 换成「遊休不動産の活用・ホテル運営受託について」。
>
> **v1.8 的变化**：① 删除了详情页右侧的「料金目安」；② 删除了隐私政策页最下方的「制定日」；
> ③ 新增独立的 **お問い合わせ 页面**（5 语言，共 55 页），首页联系区块的电话改为跳转链接，
> 表单提交后由 `_worker.js` 通过**公司自己的 Lark 邮箱 SMTP** 发邮件到 info@ele-hotel.com（详见 §11）。
>
> **v1.7 的变化**：① 6 家开业酒店换成了真实照片（外观・大堂・客室・浴室等，共 136 个 webp 文件）；
> ② 新增 5 语言的「プライバシーポリシー」页面；③ 大阪・樟葉店接入了 **予約プロ** 预约引擎（其他酒店仍走 OTA 链接）。
> 详见 **第 4 节（照片）**、**第 9 节（隐私政策）**、**第 10 节（予約プロ）**。
>
> **v1.5 最重要的变化：只写日文。**
> 以前每改一句话要在每种语言各写一遍；现在 `data/` 里**只保留日文原文**，
> 其他语言存放在翻译库 `data/i18n.json` 里，由 `build.py` 自动套用。
>
> 现有语言：**日本語 / English / 简体中文 / 繁體中文 / 한국어**（泰语已预留，未启用）。
> 详见下面 **第 8 节 多语言（必读）**。

---

## 0. 环境准备（只需一次）

需要 Python 3 和 Pillow（图片处理库）：

```bash
pip install pillow
```

日常操作只有三步：**改 JSON（只写日文）→ `python3 build.py` → 重新部署**。
生成时终端会打印缺失的译文条数，缺的地方会临时显示日文，不会出现空白。

---

## 1. 目录结构

```
ele-hotel-wood16/
├─ build.py              生成器（一般不用改）
├─ data/
│  ├─ site.json          全站文字（日文）：导航、首页文案、FAQ、公告、联系方式、品牌系列
│  ├─ hotels.json        7 家酒店的全部资料（日文）← 增删改酒店改这里
│  ├─ grand.json         Grand ELE Hotel 专页文案（日文）
│  ├─ i18n.json          翻译库：日文 → 英文 / 简中 / 繁中 / 韩文 / 泰文
│  └─ i18n-todo.json     （自动生成）待翻译清单，全部译完时不存在
├─ assets/
│  ├─ style.css          样式（配色、字体都在最上方的变量区）
│  └─ img/               所有图片（.webp）
├─ tools/import_image.py 图片转换工具
├─ tools/i18n.py         翻译引擎（不用改）
├─ tools/i18n_sync.py    翻译库管理命令行工具
├─ index.html / grand.html / privacy.html / hotels/*.html   日文版（生成物）
├─ en/ …                 英文版（生成物）
├─ zh/ …                 简体中文版（生成物）
├─ zh-hant/ …            繁体中文版（生成物）
└─ ko/ …                 韩文版（生成物）
```

`index.html`、`hotels/`、`en/`、`zh/`、`zh-hant/`、`ko/` 都是**自动生成**的，不要手改，改了会被下次
`build.py` 覆盖。

---

## 2. 增加一家新酒店

### 2-1 准备图片

```bash
python3 tools/import_image.py ~/Desktop/新酒店外观.jpg h-shibuya
```

会自动生成 `assets/img/h-shibuya.webp`（桌面用）和 `h-shibuya-sm.webp`（手机用）。
命名习惯：酒店主图用 `h-<slug>`。

### 2-2 在 `data/hotels.json` 末尾复制一份现有酒店的整段，然后按下表修改

| 字段 | 说明 |
|---|---|
| `slug` | 网址用的英文名，如 `shibuya` → 页面为 `/hotels/shibuya.html`。只能用小写字母、数字、连字符 |
| `area` | 地区筛选用：`tokyo` / `osaka` / `sendai`（若要新增地区，需在 `data/site.json` 三语区块里加一条 `filter_<地区>` 标签） |
| `img` | 主图名称，即上一步的 `h-shibuya`（不带 `.webp`） |
| `status` | `open`（运营中）或 `prep`（准备中，会显示準備中标签） |
| `rooms` | 客房总数（数字） |
| `brand` | 品牌线：`hotel` / `apart` / `onsen` / `grand` |
| `name` / `tagline` / `address` / `access` / `roomtypes` / `facilities` / `nearby` / `opened` | **只填日文**，写成 `{"ja": "…"}`。中英文之后按第 8 节补译 |
| `map_q` | Google 地图搜索用地址，空格用 `+` |
| `tel` `checkin` `checkout` `price_from` | 电话、入住/退房时间、最低价 |
| `ota` | 预约网站链接数组，每项 `{"label": "楽天トラベル", "url": "https://…"}` |
| `rtypes` | 客室タイプ区块，每个房型一行：`name`（三语）、`detail`（三语）、`cap`（人数）。每个房型对应一张占位图，有真实照片时按 2-4 替换 |

**JSON 注意点**：所有引号是英文半角 `"`，每项之间要有逗号，最后一项后面**不能**有逗号。
改完可以先检查语法：

```bash
python3 -c "import json;json.load(open('data/hotels.json'));print('OK')"
```

### 2-3 生成并检查

```bash
python3 build.py
```

新酒店会自动出现在：首页酒店列表、地区筛选、全部 5 个语言版本，并生成
`/hotels/shibuya.html`、`/en/…`、`/zh/…`、`/zh-hant/…`、`/ko/…`。

### 删除酒店

把 `data/hotels.json` 里对应的整段删掉，再删除各语言目录下的 `hotels/<slug>.html`
（`hotels/`、`en/hotels/`、`zh/hotels/`、`zh-hant/hotels/`、`ko/hotels/`），然后重新 `build.py`。

---

## 3. 修改已有酒店的文字

直接改 `data/hotels.json` 里对应酒店的字段（**只写日文**）→ `python3 build.py`。
首页文案、FAQ、公告、联系方式改 `data/site.json`；Grand 专页改 `data/grand.json`
（房型面积、餐厅时间、开业时期等目前都是计划阶段占位内容，拿到确定信息后替换这里）。

---

## 4. 用真实照片替换 AI 占位图

现在站上的图片都是 AI 生成的形象图。有真实照片时：

```bash
# 覆盖同名文件即可，页面不用改
python3 tools/import_image.py ~/照片/银座外观.jpg h-ginza-east
python3 build.py
```

**要点**

- **文件名保持不变**就能自动替换，无需改 JSON、无需改 HTML。
- 想用新名字，就把 `data/hotels.json` 里该酒店的 `img` 改成新名字。
- 建议原图**横向 3:2 或 16:10、宽度 2000px 以上**，工具会自动压到 1800px + 900px 两档。
- 支持 jpg / png / heic 转出的 jpg 等常见格式，输出统一为 webp。
- 主要图片名称对照：

| 用途 | 文件名 |
|---|---|
| 首页主视觉 | `hero-lobby` |
| 品牌系列卡片 | `brand-street`（Hotel）/ `brand-apart` / `brand-onsen` / `brand-grand` |
| 客室・設備区块 | `room-double` / `room-twin` / `breakfast` / `amenity-cafe` / `amenity-detail` |
| 各酒店主图 | `h-ginza-east` / `h-higashi-ueno` / `h-higashi-nihonbashi` / `h-shinjuku-cabin` / `h-kuzuha` / `h-sendai` |
| Grand 专页 | `grand-hero` / `grand-suite` / `grand-bath` / `grand-dining` / `grand-lounge` / `grand-facade` / `grand-detail` |

图片全部替换成真实照片后，记得把那句「本页图片为品牌形象图」的注释删掉：Grand 页在
`data/grand.json` 的 `img_note`，酒店详情页在 `data/site.json` 三语区块的
`gallery_note` / `rooms_note` / `amenity_note` / `photo_soon`。

---

## 4-B 真实照片的命名规则（v1.7 新增）

真实照片统一放在 `assets/img/`，命名规则：**`p-<酒店slug>-<用途>.webp`**（同时有 `-sm` 手机版）。

| 用途 key | 含义 |
|---|---|
| `hero` | 酒店主图（首页卡片 + 详情页大图左侧） |
| `lobby` / `front` / `exterior` / `bath` / `corridor` / `breakfast` … | 画廊右侧小图 |
| `room-single` / `room-double` / `room-twin` … | 客室タイプ卡片用 |

对应关系写在 `data/hotels.json` 每家酒店里：

```json
"img": "p-kuzuha-hero",
"gallery": [
  { "img": "p-kuzuha-front",  "label": { "ja": "フロント" } },
  { "img": "p-kuzuha-room",   "label": { "ja": "客室" } }
],
"rtypes": [
  { "name": { "ja": "シングル" }, "img": "p-kuzuha-room-single", "cap": 1 }
]
```

- `gallery` 支持 **4～6 张**（含主图），删掉 `gallery` 整段就回到占位图。
- 房型里**不写 `img`** 就显示「写真準備中」占位图 —— 没有对应照片时请保持不写，不要用别的房型照片顶替。
- 全部房型都有照片时，`data/site.json` 的 `rooms_note_partial`（“一部の客室タイプは写真準備中です”）不会显示，无需手动删。

### 批量导入照片

把照片按酒店分文件夹（文件名随意），然后：

```bash
python3 tools/import_image.py ~/照片/kuzuha/フロント.jpg p-kuzuha-front
python3 build.py
```

一次多张时可以照抄仓库里的 `import_photos.py`（源码包内附），改一下文件夹路径即可。

---

## 5. 改配色 / 字体

`assets/style.css` 最上方的 `:root` 变量区：`--bg`、`--surface`、`--text`、`--primary`、
品牌色 `--b-hotel` / `--b-apart` / `--b-onsen` / `--b-grand` 等。深色模式在
`[data-theme="dark"]` 区块。改完 `python3 build.py`（样式链接带内容哈希，会自动破缓存）。

---

## 6. 发布

```bash
python3 build.py      # 生成
git add -A && git commit -m "更新银座店照片"   # 留个版本记录（可选但推荐）
```

然后重新部署（见下节）。Logo 图形已注册商标，**只可改颜色，不要修改图形本身**。

---

## 7. 部署

本站是纯静态文件，把整个目录（除 `build.py` / `data/` / `tools/` 之外也一并上传无妨）
传到任意静态托管即可。

- **Vercel（推荐，可绑 `www.ele-hotel.com`）**：在这个对话里连接 Vercel 连接器后，
  我可以直接把当前构建结果部署到你的 Vercel 项目，再把域名指向它。
  域名侧只需在 DNS 上加一条 `www` 的 CNAME 指到 Vercel 给出的地址，
  根域 `ele-hotel.com` 用 A 记录指向 `76.76.21.21`，并在 Vercel 里设为跳转到 www。
- **pplx.app 预览链接**：适合内部确认，不能绑自有域名。
- 注意：静态托管上 `/zh/` 这种目录形式的网址可能 404，请用 `/zh/index.html`。
  部署到 Vercel 后可通过配置 `cleanUrls` / 目录索引解决。

---

## 8. 多语言（必读）

### 8-1 原理

`data/` 里只写**日文原文**。生成时 `build.py` 会拿每一句日文去 `data/i18n.json`
里查对应的中文 / 英文；查到就用译文，查不到就先显示日文，并在终端提示还差几条。

```
data/site.json（日文）  ──┐
data/hotels.json（日文）──┼──►  build.py  ──►  ja / zh / en 页面
data/grand.json（日文） ──┘         ▲
                          data/i18n.json（译文库）
```

翻译库里每条长这样（`locked: true` 表示人工确认过的定稿，工具不会覆盖）：

```json
"0537e672b005dab0": {
  "ja": "言語を選ぶ",
  "zh": "选择语言",
  "en": "Choose language",
  "ko": "", "th": "",
  "locked": true
}
```

### 8-2 改一句话的完整流程

1. 在 `data/*.json` 里改**日文**。
2. `python3 build.py` → 终端会提示 `missing translations: {'en': 1, 'zh': 1, …}`，
   并生成 `data/i18n-todo.json`。
3. 打开 `data/i18n-todo.json`，把 `en` / `zh` / `zh-Hant` / `ko` 填上。
4. `python3 tools/i18n_sync.py --import data/i18n-todo.json`
5. `python3 build.py` → 显示 `translations complete`，完成。

只改数字、时间、价格、地址这类中日英通用的内容，第 2 步不会报缺失，直接跳到第 5 步即可。

### 8-3 常用命令

```bash
python3 tools/i18n_sync.py --stats        # 看各语言完成度
python3 tools/i18n_sync.py --todo en zh zh-Hant ko   # 导出待翻译清单
python3 tools/i18n_sync.py --import data/i18n-todo.json   # 导入译文
python3 tools/i18n_sync.py --prune        # 清掉已不再使用的旧译文
```

导入前会自动备份成 `data/i18n.json.bak`。要覆盖已定稿的条目加 `--force`。

### 8-4 繁体中文是怎么来的

繁体版不是重新翻译的，而是用 OpenCC（`s2twp`，台湾正体+台港惯用词）从简体自动转换，
再套一份人工用词修正表（酒店→飯店、信息→資訊、前台→櫃台、日元→日圓、运营→營運、
智能马桶→免治馬桶、设备→設備 等）。

所以**改中文时只要改简体那一份**，繁体可以用下面的命令一键重新生成：

```bash
python3 tools/zh_hant.py       # 由简体重算全部繁体译文
```

如果某一条繁体需要跟简体不同的说法，手工改 `data/i18n.json` 里那条的 `zh-Hant`
并加 `"hant_manual": true`，重算时会跳过它。

### 8-5 增加泰语（或其他语言）

框架已经预留，`data/site.json` 顶部的 `langs` 里 th 目前是 `"enabled": false`。
上线新语言的步骤：

1. `python3 tools/i18n_sync.py --todo th`，把 `data/i18n-todo.json` 里的 `th` 填满
   （约 360 条，可交给翻译公司或先机翻再人工校对）。
2. `python3 tools/i18n_sync.py --import data/i18n-todo.json`
3. 把 `data/site.json` 里 th 那一条改成 `"enabled": true`。
4. `python3 build.py` → 自动生成 `/th/` 全站，右上角地球菜单里自动多出「ไทย」，
   字体也会自动切换（Noto Sans Thai）。

反之，把 `enabled` 改回 `false` 并删掉对应目录即可下线该语言。

### 8-6 注意

- 不要手改 `data/i18n.json` 里的 `ja` 字段——那是索引键的来源，改了会丢译文。
  要改原文请改 `data/*.json`，然后按 8-2 补译。
- 同一句日文在不同位置需要不同译法时，翻译库会自动生成带 `path` 的独立条目，
  不用手工处理。


---

## 9. プライバシーポリシー（隐私政策页）

- 页面：`privacy.html` / `en/privacy.html` / `zh/privacy.html` / `zh-hant/privacy.html` / `ko/privacy.html`（自动生成）。
- 入口：每个页面页脚「インフォメーション」栏最后一条。
- 正文在 `data/site.json` 的 `privacy` 段：`title`、`intro`、`sections[]`（每段 `h` 标题 + `p` 段落 + `list` 列表）、
  `contact_rows`（公司名 / 所在地 / 电话 / 受理时间）、`updated`（制定日）。**只写日文**，其他语言按第 8 节补译。
- 署名主体为**株式会社TEJ**（本社：〒104-0032 東京都中央区八丁堀3-18-9 第2直平ビル2階 / TEL 03-6260-8831）。
- 条文参考了 hotel-monday.com 与 tej.jp 的结构，共 10 条。**若今后委托律师审阅并修改，请只改 `data/site.json`，不要改 HTML。**

---

## 10. 予約プロ 预约引擎（目前只用于大阪・樟葉店）

配置写在 `data/hotels.json` 里 kuzuha 这家酒店的 `booking` 段：

```json
"booking": {
  "engine": "yoyakupro",
  "action": "https://www7.489pro.com/asp/489/menu.asp?id=27000054&ty=ser",
  "top":    "https://www7.489pro.com/asp/489/menu.asp?id=27000054",
  "max_guests": 5,
  "max_nights": 5,
  "max_rooms": 5
}
```

- `id=27000054` 是樟葉店在予約プロ的**施设 ID**。别的酒店签约后，把这段整体复制到那家酒店里，只改 `id`。
- 详情页右侧会显示「空室検索・ご予約」表单（入住日 + 人数 + 泊数 + 部屋数），点「空室を検索」在**新窗口**打开予約プロ的空房结果页；下面还有一个「公式サイトから予約する」按钮直接进预约首页。
- 没有 `booking` 段的酒店，右侧显示的是 OTA（楽天 / じゃらん / 公式サイト）链接列表 —— 现状即如此。
- 予約プロ的后台账号密码由酒店侧管理，**网站本身不保存任何账号信息**，也不需要 API key。
- 首页搜索框（顶部那个）目前是引导性的，会提示前往樟葉店页面预约；今后各店都接入引擎后可改为直连。


---

## 11. お問い合わせ 表单（v1.8 新增）

### 11-1 页面

- 地址：`/contact.html`、`/en/contact.html`、`/zh/contact.html`、`/zh-hant/contact.html`、`/ko/contact.html`
- 文案全部写在 `data/site.json` → `ja.contact_page` 里（标题、说明、各字段名、按钮、错误提示、右侧三个说明框）。
  改完跑一次 `python3 build.py`，然后按 §8 的流程补其他语言的翻译。
- 「お問い合わせ種別」的下拉选项 = `ja.contact_page.kinds` 数组，直接加减即可。
- 首页的联系区块不再放电话，改成一个跳转到本页的链接（文案 = `ja.contact_cta`）。
  电话号码现在只出现在本页右侧的「お電話でのお問い合わせ」框里，以及各酒店详情页。
- 表单在浏览器端会先自检：必填项、邮箱格式、隐私政策勾选；还带一个隐藏的防机器人字段。

### 11-2 邮件是怎么发出去的

站点根目录下的 **`_worker.js`** 是 Cloudflare Pages 的「高级模式」入口：
除了 `POST /api/contact` 以外的所有请求都原样交给静态文件，`/api/contact` 则负责发邮件。

收件人默认 **info@ele-hotel.com**，回信地址自动设为访客填写的邮箱（在邮件客户端直接点「回复」即可）。

支持三种发信通道，`_worker.js` 会按顺序挑第一个配置齐全的用：

| 优先级 | 通道 | 需要的环境变量 |
| --- | --- | --- |
| A（推荐） | **公司自己的 Lark（飞书）邮箱 SMTP** | `SMTP_HOST` `SMTP_PORT` `SMTP_USER` `SMTP_PASS` |
| B | Resend | `RESEND_API_KEY` |
| C | Brevo | `BREVO_API_KEY` |

**一个都不配的话，表单会一直提示「送信に失敗しました」。**

#### A. 用公司自己的邮箱（不需要改任何 DNS）

我们的域名邮箱跑在 Lark 上，SPF / DKIM 本来就由 Lark 管理，所以用 `@ele-hotel.com`
作发件人天生就通过验证 —— **不用新增 TXT，也不用碰 MX。**

**第 1 步｜在 Lark 后台建一个公共邮箱**

1. 打开 [Lark 管理后台](https://www.larksuite.com/admin) → 产品设置 → **邮箱**
2. 地址管理 → **公共邮箱** → 创建公共邮箱
3. 地址填 `noreply@ele-hotel.com`（名称随意，比如「网站表单」）
4. **务必勾上「启用 IMAP/SMTP 服务」**
5. 创建后点「编辑」，把系统生成的 **IMAP/SMTP 专用密码**记下来
   （这个密码跟登录密码不是一回事；忘了可以重新生成）

> 也可以直接用现有的 info@ele-hotel.com 开 SMTP，但用独立的 noreply 更干净：
> 万一密码泄露，只影响这个表单。

**第 2 步｜在 Cloudflare 填环境变量**

Cloudflare Dashboard → Workers & Pages → `ele-hotel` → Settings →
Variables and Secrets → 逐条添加（`SMTP_PASS` 请选 **Secret / 加密**类型）：

| 变量名 | 值 |
| --- | --- |
| `SMTP_HOST` | `smtp.larksuite.com`（若用国内版飞书则填 `smtp.feishu.cn`） |
| `SMTP_PORT` | `465` |
| `SMTP_USER` | `noreply@ele-hotel.com` |
| `SMTP_PASS` | 第 1 步拿到的 IMAP/SMTP 专用密码 |

可选：

| 变量名 | 默认值 | 说明 |
| --- | --- | --- |
| `CONTACT_TO` | `info@ele-hotel.com` | 收件人 |
| `CONTACT_FROM` | `ELE HOTEL <SMTP_USER 的地址>` | 发件人显示名 |

> **端口一定用 465**（隐式 TLS）。587 的 STARTTLS 在 Cloudflare 运行时上有已知
> 兼容问题，代码虽然写了 587 的分支，但不保证稳。25 端口 Cloudflare 直接封禁。
>
> Lark 公共邮箱的 SMTP 发信有频率上限（大致每天数百封），做联系表单完全够用。

#### B / C. 第三方邮件 API（备选）

如果哪天不想用自家 SMTP，去 [resend.com](https://resend.com) 或 [brevo.com](https://brevo.com)
注册拿一个 API Key，填成 `RESEND_API_KEY` 或 `BREVO_API_KEY` 即可，代码会自动切过去。
注意这条路要给发件域名加 DKIM 验证记录 —— 那时**只新增服务商指定的子记录，
绝对不要改动现有的 MX 和 SPF(TXT)**，公司邮箱在飞书上跑着。

#### 部署时的注意点（重要）

`_worker.js` **不能当普通静态文件上传**。用 Direct Upload API 部署时，它必须作为
deployment 的独立表单字段提交：

```
_worker.js=@<路径>/_worker.js;type=application/javascript+module
```

`cf_deploy.py` 已经处理好了（`WORKER` 变量 + 第 6 步 form 字段），并且已把它从
静态资源清单里排除。如果误当静态文件上传，表现是 **POST /api/contact 返回 405**、
表单永远提示送信失败。

### 11-3 自测

配置好 Key 之后，打开 https://www.ele-hotel.com/contact 填一遍提交，
应该看到绿色的「送信が完了しました」，同时 info@ele-hotel.com 收到一封
标题为「【ELE HOTEL お問い合わせ】…」的邮件。

如果报错，去 Cloudflare Dashboard → `ele-hotel` → Logs（实时日志）里看
`contact mail failed:` 后面的原因，常见的几种：

| 日志里出现 | 原因 |
| --- | --- |
| `smtp 535` | SMTP 用户名或专用密码不对 |
| `no_mail_transport_configured` | 四个 SMTP 变量没填全（或名字拼错） |
| `smtp 550` / `553` | 发件地址跟 `SMTP_USER` 不一致，Lark 拒绝代发 |
| 连接超时 | 端口填了 25，或 `SMTP_HOST` 写错 |


---

## 12. 设施 / アメニティ 图标（v1.9）

### 12-1 怎么改一家酒店的图标

打开 `data/hotels.json`，找到那家酒店，里面有两个数组：

```json
"facilities": ["全室無料Wi-Fi", "24時間フロント", "全室禁煙", "液晶テレビ",
               "冷蔵庫", "電気ケトル", "洗浄機能つきトイレ", "各室バス・トイレ"],
"amenities":  ["スリッパ", "ドライヤー", "タオル", "シャンプー",
               "コンディショナー", "ボディシャンプー", "館内着"]
```

- `facilities` = 上面那组「客室設備・館内設備」，**按数组顺序显示**，所以直接改顺序即可。
- `amenities` = 下面那组「備品・アメニティ」，7 家店目前都一样。
- 只写日文，其他 4 种语言由 §8 的翻译库自动补。新写的词第一次会进
  `data/i18n-todo.json`，翻译好后 `python3 tools/i18n_sync.py --import <文件>` 导入。

### 12-2 统一顺序（约定）

```
全室無料Wi-Fi → 24時間フロント → 全室禁煙 → 液晶テレビ
→〔冷蔵庫（有则加）〕→ 電気ケトル → 洗浄機能つきトイレ
→〔コインランドリー（有料）/ 大型ロッカー / 共用シャワー・バスルーム（有则加）〕
→ 各店特有项目（朝食無料・カフェ・ズボンプレッサー 等）
```

新宿 Cabin 店是舱型，只写实际有的：全館無料Wi-Fi・24時間フロント・全館禁煙・
大型ロッカー・共用シャワー・バスルーム。

### 12-3 图标是怎么匹配的

`amenity_icons.py` 里有一张 `RULES` 表（关键词 → 图标名），从上往下匹配第一个命中的：

```python
RULES = [
    (("スリッパ",), "slipper"),
    (("ドライヤー",), "dryer"),
    (("コンディショナー", "リンス"), "conditioner"),
    ...
]
```

- 加新词又不想画新图标：把关键词加进已有图标那一行即可。
- 要画新图标：在 `ICONS` 字典里加一个 24×24 视口的线性 SVG（`stroke` 用
  `currentColor`，不要写 `fill`），然后在 `RULES` 里加一行。
- 注意顺序：更具体的词要放在更宽泛的词**前面**（「コンディショナー」必须排在
  「シャンプー」前面，否则会被后者抢先命中）。
- 改完 `python3 build.py`，随便打开一个详情页确认图标对不对。
